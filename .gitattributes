* text=auto eol=lf
*.sh text eol=lf
entrypoint.sh text eol=lf

# C-Gate data files — store byte-for-byte, no EOL normalisation
*.dat -text
# Windows batch scripts keep CRLF on checkout
*.bat text eol=crlf
